{{> __newnav}}

<style>
  :root {
    --home-primary: #1565c0;
    --home-primary-dark: #0d47a1;
    --home-surface: #0c1727;
    --home-muted: #5f738c;
    --home-card: #0f1f35;
    --home-border: rgba(255, 255, 255, 0.08);
  }

  .home-shell {
    background:
      radial-gradient(circle at 10% 20%, rgba(21, 101, 192, 0.25), transparent 35%),
      radial-gradient(circle at 90% 10%, rgba(21, 101, 192, 0.15), transparent 30%),
      linear-gradient(135deg, #0a1220 0%, #0b1628 50%, #0c1727 100%);
    color: #e8edf5;
    min-height: 100vh;
    padding-bottom: 72px;
  }

  .home-shell .mui-container {
    max-width: 1180px;
  }

  .home-hero {
    padding: 72px 0 48px;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 32px;
    align-items: center;
  }

  .hero-card {
    background: var(--home-card);
    border-radius: 18px;
    border: 1px solid var(--home-border);
    box-shadow: 0 18px 60px rgba(0, 0, 0, 0.35);
  }

  .chip {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 14px;
    background: rgba(21, 101, 192, 0.12);
    border-radius: 999px;
    color: #d0e6ff;
    font-weight: 600;
  }

  .headline {
    font-size: clamp(32px, 4vw, 46px);
    font-weight: 800;
    line-height: 1.1;
    margin: 14px 0;
  }

  .lede {
    color: var(--home-muted);
    font-size: 17px;
  }

  .hero-actions .mui-btn {
    text-transform: none;
    font-weight: 700;
    border-radius: 12px;
    padding: 12px 18px;
  }

  .meta-row {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 18px;
  }

  .meta-card {
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid var(--home-border);
    border-radius: 12px;
    padding: 12px 14px;
    min-width: 140px;
  }

  .feature-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 18px;
  }

  .feature-card {
    background: var(--home-card);
    border: 1px solid var(--home-border);
    border-radius: 14px;
    padding: 18px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.25);
  }

  .feature-card h4 {
    margin: 10px 0 6px;
  }

  .bullet {
    display: flex;
    gap: 10px;
    align-items: flex-start;
    margin: 8px 0;
    color: var(--home-muted);
  }

  .process {
    margin: 52px 0 34px;
  }

  .process-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 16px;
  }

  .step-card {
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid var(--home-border);
    border-radius: 14px;
    padding: 16px;
  }

  .step-chip {
    background: rgba(255, 255, 255, 0.12);
    padding: 6px 12px;
    border-radius: 999px;
    font-weight: 700;
    display: inline-block;
    margin-bottom: 8px;
  }

  .about-preview {
    margin: 40px 0;
    background: linear-gradient(135deg, rgba(21, 101, 192, 0.25), rgba(21, 101, 192, 0.05));
    border-radius: 18px;
    border: 1px solid var(--home-border);
    padding: 24px;
  }

  .blog-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 16px;
    margin-top: 18px;
  }

  .blog-card {
    background: var(--home-card);
    border: 1px solid var(--home-border);
    border-radius: 14px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
  }

  .blog-card img {
    width: 100%;
    height: 160px;
    object-fit: cover;
    background: #0e1a2b;
  }

  .blog-card .body {
    padding: 14px 14px 16px;
  }

  .blog-card h5 {
    margin: 0 0 6px;
  }

  .cta-panel {
    margin-top: 40px;
    background: var(--home-card);
    border: 1px solid var(--home-border);
    border-radius: 16px;
    padding: 20px;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    align-items: center;
    gap: 12px;
  }

  @media (max-width: 767px) {
    .home-hero {
      padding-top: 42px;
    }
  }
</style>

<main class="home-shell">
  <section class="mui-container home-hero">
    <div class="hero-copy">
      <span class="chip"><span class="material-symbols-rounded">home</span> Florida cash buyers</span>
      <h1 class="headline">Sell fast with a MUI-inspired, worry-free experience</h1>
      <p class="lede">We structure every offer with clarity, transparency, and a design-first flow so you always
        know where your sale stands. No repairs. No showings. Just a professional cash offer that respects your time.</p>
      <div class="hero-actions">
        <a class="mui-btn mui-btn--primary mui--text-white" href="/forms/fastSell">Get my cash offer</a>
        <a class="mui-btn mui-btn--flat mui-btn--primary" href="/properties">Browse current listings</a>
      </div>
      <div class="meta-row">
        <div class="meta-card">
          <div class="mui--text-headline">24h</div>
          <div class="mui--text-caption">Offer turnaround</div>
        </div>
        <div class="meta-card">
          <div class="mui--text-headline">Zero Fees</div>
          <div class="mui--text-caption">We cover closing</div>
        </div>
        <div class="meta-card">
          <div class="mui--text-headline">Local</div>
          <div class="mui--text-caption">Tampa-based team</div>
        </div>
      </div>
    </div>
    <div class="hero-card mui-panel">
      <div class="mui--text-title">Your modern selling path</div>
      <p class="mui--text-body2">A clean, MUI-driven checklist keeps the process predictable.</p>
      <div class="bullet">
        <span class="material-symbols-rounded">bolt</span>
        <div>
          <strong>Start in minutes.</strong> Share your property basics and let us handle the heavy lifting.
        </div>
      </div>
      <div class="bullet">
        <span class="material-symbols-rounded">receipt_long</span>
        <div>
          <strong>Transparent offers.</strong> We break down pricing, repairs, and timing with no fine print.
        </div>
      </div>
      <div class="bullet">
        <span class="material-symbols-rounded">calendar_month</span>
        <div>
          <strong>You pick the date.</strong> Close when you are ready—it can be as fast as 7 days.
        </div>
      </div>
      <a class="mui-btn mui-btn--accent mui--text-white" href="/forms/fastSell">Start a stress-free sale</a>
    </div>
  </section>

  <section class="mui-container">
    <div class="feature-grid">
      <article class="feature-card">
        <span class="material-symbols-rounded">handshake</span>
        <h4>Human help, tech-backed</h4>
        <p class="mui--text-body2">Talk to a real specialist while our MUI-like workflow keeps every step organized and documented.</p>
      </article>
      <article class="feature-card">
        <span class="material-symbols-rounded">verified_user</span>
        <h4>No repairs, no surprises</h4>
        <p class="mui--text-body2">We purchase as-is. Keep your timeline intact and avoid unexpected deductions at closing.</p>
      </article>
      <article class="feature-card">
        <span class="material-symbols-rounded">query_stats</span>
        <h4>Data-forward valuations</h4>
        <p class="mui--text-body2">Localized comps, retrofit costs, and demand signals shape a fair offer you can validate.</p>
      </article>
    </div>
  </section>

  <section class="mui-container process">
    <div class="mui--text-display1 mui--text-white">How we deliver</div>
    <p class="mui--text-body2" style="color: var(--home-muted);">Clarity at each milestone, designed with material simplicity.</p>
    <div class="process-grid">
      <div class="step-card">
        <div class="step-chip">Step 1</div>
        <div class="mui--text-title">Share your address</div>
        <p class="mui--text-body2">Give us the basics—we combine them with local data to craft your first estimate.</p>
      </div>
      <div class="step-card">
        <div class="step-chip">Step 2</div>
        <div class="mui--text-title">Review the cash offer</div>
        <p class="mui--text-body2">Walk through each line item with us. We answer questions live and adjust to your needs.</p>
      </div>
      <div class="step-card">
        <div class="step-chip">Step 3</div>
        <div class="mui--text-title">Pick your close date</div>
        <p class="mui--text-body2">Coordinated closing with digital paperwork and no out-of-pocket costs.</p>
      </div>
    </div>
  </section>

  <section class="mui-container about-preview">
    <div class="mui-row">
      <div class="mui-col-md-9">
        <div class="mui--text-title">Meet Nick House Buyer</div>
        <p class="mui--text-body2">We're a Florida-first team using a MUI-inspired playbook to keep selling simple, respectful, and fast. Learn how we blend people-first service with modern tooling.</p>
      </div>
      <div class="mui-col-md-3 mui--text-right">
        <a class="mui-btn mui-btn--primary mui--text-white" href="/about">Explore our story</a>
      </div>
    </div>
  </section>

  {{#if blogs}}
    <section class="mui-container">
      <div class="mui--text-title mui--text-white">Latest insights</div>
      <div class="blog-grid">
        {{#each blogs}}
          <article class="blog-card">
            {{#if Imag}}
              <img src="/{{Imag}}" alt="{{Title}}">
            {{else}}
              <img src="/assets/img/house.jpg" alt="{{Title}}">
            {{/if}}
            <div class="body">
              <h5>{{Title}}</h5>
              <p class="mui--text-body2" style="color: var(--home-muted);">{{Description}}</p>
              <a class="mui-btn mui-btn--flat mui-btn--primary" href="/blog/{{postId}}">Read article</a>
            </div>
          </article>
        {{/each}}
      </div>
    </section>
  {{/if}}

  <section class="mui-container cta-panel">
    <div>
      <div class="mui--text-title">Ready for a MUI-clean, fast sale?</div>
      <p class="mui--text-body2" style="color: var(--home-muted); margin: 4px 0;">Tell us about your home and get a clear, written cash offer today.</p>
    </div>
    <div class="mui--text-right">
      <a class="mui-btn mui-btn--primary mui--text-white" href="/forms/fastSell">Start my offer</a>
    </div>
  </section>
</main>

{{> __newfooter}}
